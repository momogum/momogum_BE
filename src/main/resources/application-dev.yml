spring:
  application:
    name: momogum

  config:
    activate:
      on-profile: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://momogum-db.cp060444k9y4.ap-northeast-2.rds.amazonaws.com:3306/momogum
    username: root
    password: l9HkhyYwifAwnoe

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: update
        default_batch_fetch_size: 1000